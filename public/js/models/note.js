define(function(require){
  var
    // 3rd Party Dependencies

    // App Dependencies
    utils = require('utils')

    // Module Variables
  ;

  return utils.Mvc.Model.extend({
    defaults: {

    }
  });
});
