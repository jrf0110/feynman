define(function(require){
  var
    // 3rd Party Dependencies
    $       = require('jquery')

    // App Dependencies
  , utils   = require('utils')
  , config  = require('config')

    // Module Variables
  ;

  return utils.View.extend({

  });
});
